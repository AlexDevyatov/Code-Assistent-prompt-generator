# Быстрый старт: Интеграция MCP-Weather

## 🚀 Шаг 1: Запустите ваш MCP-Weather сервер

### Вариант A: HTTP сервер (рекомендуется)

```bash
cd /path/to/MCP-Weather
python3 server_remote.py --host 0.0.0.0 --port 9001
```

Сервер будет доступен по адресу: `http://localhost:9001` (или `http://127.0.0.1:9001`)

### Вариант B: Локальный stdio сервер

```bash
cd /path/to/MCP-Weather
python3 server.py
```

## 🔧 Шаг 2: Настройте DeepseekWebClient

Файл `.env` уже обновлен с настройками по умолчанию:

```env
MCP_WEATHER_SERVER_URL=http://127.0.0.1:9001
MCP_USE_HTTP=true
```

**Если ваш сервер запущен на другом адресе**, измените `MCP_WEATHER_SERVER_URL` в `.env`:

```env
# Для локального сервера:
MCP_WEATHER_SERVER_URL=http://localhost:9001

# Для сервера на той же машине (как у backend):
MCP_WEATHER_SERVER_URL=http://127.0.0.1:9001
```

## ▶️ Шаг 3: Запустите DeepseekWebClient

```bash
npm run dev
```

## ✅ Шаг 4: Проверьте подключение

### Через API:

```bash
# Проверить список инструментов
curl http://localhost:8000/api/mcp/list-tools/mcp-weather

# Проверить работу чата о погоде
curl -X POST "http://localhost:8000/api/weather-chat" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Какая погода в Москве?"}'
```

### Через веб-интерфейс:

1. **Просмотр инструментов**: http://localhost:8000/mcp-server
   - Введите: `mcp-weather`
   - Нажмите "Подключиться"

2. **Чат о погоде**: http://localhost:8000/weather-chat
   - Задайте вопрос: "Какая погода в Москве?"
   - Или: "Прогноз погоды в Санкт-Петербурге на 5 дней"

## 🐛 Устранение неполадок

### Сервер не отвечает

1. Проверьте, что MCP-Weather сервер запущен:
   ```bash
   curl http://localhost:9001/health
   ```

2. Проверьте логи MCP-Weather сервера

3. Проверьте, что порт не заблокирован файрволом

### Ошибка подключения

1. Убедитесь, что URL в `.env` правильный
2. Проверьте, что `MCP_USE_HTTP=true` для HTTP сервера
3. Проверьте логи FastAPI сервера

### Инструменты не вызываются

1. Проверьте формат ответа сервера (должен быть JSON-RPC 2.0)
2. Убедитесь, что сервер поддерживает endpoint `/messages/`
3. Проверьте логи на наличие ошибок

## 📚 Дополнительная информация

- Полная документация: [MCP_WEATHER_INTEGRATION.md](MCP_WEATHER_INTEGRATION.md)
- Репозиторий MCP-Weather: https://github.com/AlexDevyatov/MCP-Weather
