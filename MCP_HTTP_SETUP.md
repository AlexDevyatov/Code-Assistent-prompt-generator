# Настройка MCP сервера через HTTP

## Конфигурация

MCP сервер погоды настроен на использование HTTP подключения по адресу: `http://185.28.85.26:8001`

### Переменные окружения

В файле `.env` можно настроить следующие параметры:

```env
# URL MCP сервера погоды
MCP_WEATHER_SERVER_URL=http://185.28.85.26:8001

# Использовать HTTP подключение (true/false)
MCP_USE_HTTP=true
```

### Значения по умолчанию

Если переменные окружения не заданы, используются значения по умолчанию:
- `MCP_WEATHER_SERVER_URL=http://185.28.85.26:8001`
- `MCP_USE_HTTP=true`

## Как это работает

1. **HTTP подключение** (по умолчанию):
   - Запросы к MCP серверу отправляются через HTTP POST
   - Используется JSON-RPC 2.0 протокол
   - Все запросы идут на `http://185.28.85.26:8001`

2. **Локальное подключение** (если `MCP_USE_HTTP=false`):
   - Используется stdio транспорт
   - MCP сервер должен быть установлен локально
   - Работает через процессы и стандартные потоки ввода/вывода

## Логирование

При использовании HTTP подключения в логах будут видны сообщения:
- `🌐 Using HTTP connection to MCP server: http://185.28.85.26:8001`
- `🌐 Calling MCP via HTTP: ...`

При использовании локального подключения:
- `🔧 Using local stdio connection to MCP server: mcp-weather`

## Отладка

Если возникают проблемы с подключением:

1. Проверьте доступность сервера:
   ```bash
   curl http://185.28.85.26:8001
   ```

2. Проверьте логи приложения на наличие ошибок HTTP подключения

3. Убедитесь, что сервер поддерживает JSON-RPC 2.0 протокол
